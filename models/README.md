# Coach Kata Models

This package bundles ML model code for Coach Kata and depends on the shared `coachkata-libraries`. It is installed editable when you run `uv run poe jupyter` so notebooks can import it directly.

Minimum contents to satisfy packaging tools. Update with model-specific docs as needed.

